# Path to the SQLite file backing the registry when running the optional
# HTTP API (prompt_registry.api). No secrets are required to run this
# project -- this file exists purely for configuring local storage paths.
PROMPT_REGISTRY_DB=./prompt_registry.db
